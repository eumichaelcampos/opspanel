# API URL for Next.js rewrites (server-side). In production, internal URL to API container/service.
API_URL=http://localhost:3001

# Public GitHub repo shown on landing page (org/repo)
NEXT_PUBLIC_GITHUB_REPO=your-org/opspanel
